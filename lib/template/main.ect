<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="<%= @description %>">
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=4,user-scalable=yes" />

    <% if @twitter != "" || @facebook != "" : %>
      <% if @ogpimg != "" : %>
        <meta property="og:image" content="<%= @site_name %>/<%= @ogpimg %>" />
      <% end %>
      <meta property="og:url" content="<%= @start_url %>" />
      <meta property="og:type" content="website" />
      <meta property="og:title" content="<%= @title %>" />
      <meta property="og:description" content="<%= @description %>" />
      <meta property="og:site_name" content="<%= @site_name %>" />

      <% if @twitter != "" : %>
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:site" content="@<%= @twitter %>" />
      <% end %>
      <% if @facebook != "" : %>
        <meta property="fb:app_id" content="<%= @facebook %>" />
      <% end %>
    <% end %>

    <title><%= @title %></title>
    <% if @favimg != "" : %>
      <link rel="icon" href="<%= @favimg %>" />
      <link rel="apple-touch-icon" href="<%= @favimg %>">
    <% end %>

    <% if @PWA == "activate" : %>
      <link rel="manifest" href="<%= @manifest %>" />
    <% end %>

    <link rel="stylesheet" type="text/css" href="<%= @systemcss %>" />
    <% for f in @cssfilelist : %>
      <link rel="stylesheet" type="text/css" href="<%= f %>" />
    <% end %>

    <script type="text/javascript">
      NODE_ENV = "<%= @NODE_ENV %>";
      pkgname = "<%= @pkgname %>";
      PWA = "<%= @PWA %>";
      SITE_WIDTH = "<%= @site_width %>";
      SITE_HEIGHT = "<%= @site_height %>";
    </script>

    <script type="text/javascript" src="<%= @site_name %>/syslib/plustick.min.js" defer></script>
    <% for f in @jssyslist : %>
      <script async type="text/javascript" src="<%= f %>" defer></script>
    <% end %>
  </head>
  <body style="margin: 0px 0px 0px 0px; overflow:hidden; background-color:<%= @bgcolor %>; overscroll-behavior:none;">
    <script type="text/javascript" src="<%= @site_name %>/syslib/main.min.js" defer></script>
    <script type="text/javascript" src="<%= @site_name %>/view/appsmain.min.js" defer></script>
  </body>
</html>

